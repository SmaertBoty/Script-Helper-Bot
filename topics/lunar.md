Lunar client uses a different directory from regular clients.

If you have a pre-existing minecraft instance:
Copy everything from `%APPDATA%\.minecraft\minescript\`
To `~/.lunarclient/offline/multiver/`

and set up `config.txt`
